import UIKit
import XCTest
import Quick
import Nimble
import MinesOfMios

class MinessOfMiosSpec: QuickSpec {
    override func spec() {
        describe("a spec") {
            it("should have been wrote") {
                expect(true).to(beFalsy())
            }
        }
    }
}